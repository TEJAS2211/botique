apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis-cart
spec:
  template:
    metadata:
      annotations:
        sidecar.istio.io/inject: null
    spec:
      containers:
      - name: redis
